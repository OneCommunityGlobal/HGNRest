//  Testing for the final email draft that manager will receive on cronjob
// const express = require('express');

// const router = express.Router();
// // const { emailWeeklySummariesForAllUsers } = require('../helpers/userHelper')();
// const userHelper = require('../helpers/userHelper')();

// router.get('/test-weekly-summaries', async (req, res) => {
//   try {
//     await userHelper.emailWeeklySummariesForAllUsers(1);  // last week
//     res.send('✅ Weekly summaries test email triggered');
//   } catch (err) {
//     console.error(err);
//     res.status(500).send('❌ Failed to send test email');
//   }
// });

// module.exports = router;
